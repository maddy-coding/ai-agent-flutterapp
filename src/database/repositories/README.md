This folder is reserved for WatermelonDB repository adapters once the project is prebuilt.
The UI currently reads from mocked connector services so the product flow can be explored before native database wiring is installed.
